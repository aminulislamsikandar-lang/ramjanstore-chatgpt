import {useEffect,useState}from"react";import type {Product}from"@ramjanstore/types";import{engagementApi}from"../services/engagementApi";import{useCartStore}from"../lib/cartStore";
export default function Wishlist(){const[items,setItems]=useState<Product[]>([]);const add=useCartStore(s=>s.addItem);useEffect(()=>{void engagementApi.wishlistItems().then(r=>setItems(r.data)).catch(()=>setItems([]))},[]);return <main className="mx-auto max-w-6xl p-4 sm:p-8"><h1 className="text-3xl font-bold">Wishlist</h1>{!items.length?<p className="mt-8 text-slate-500">Your wishlist is empty.</p>:<div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{items.map(p=><article key={p.id} className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="aspect-square bg-slate-100">{p.images?.[0]?.url&&<img src={p.images[0].url} alt={p.name} className="h-full w-full object-cover"/>}</div><div className="p-4"><h2 className="font-semibold">{p.name}</h2><p className="mt-1">₹{p.price}</p><button onClick={()=>add({productId:p.id,name:p.name,unitPrice:p.price,quantity:1,imageUrl:p.images?.[0]?.url})} className="mt-4 w-full rounded-xl bg-slate-900 px-4 py-2 text-white">Add to cart</button></div></article>)}</div>}</main>}
